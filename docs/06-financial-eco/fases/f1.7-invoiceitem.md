# F1.7 — Dominio `invoiceitem` (Línea de factura)

> **Sub-fase:** F1.7 · **Esfuerzo:** Medio · **Patrón:** `docs/02-patrones/05-odata-module-pattern.md`
> **Depende de:** F1.4, F1.5.

## 0. Objetivo

Crear `src/core/finance/invoiceitem/` — posición/línea de factura, enlazada a `invoice` y `glaccount`.
Cierra las navegaciones `@HasMany` en Invoice y GlAccount.

## 1. Modelo `src/core/finance/invoiceitem/model/invoiceitem.odata.model.ts`

```typescript
@Table({ tableName: "invoiceitems", timestamps: true })
export class InvoiceItemOData extends Model<InvoiceItemOData> {
    @Column({ dataType: DataTypes.STRING, isPrimaryKey: true }) id!: string;
    @Column({ dataType: DataTypes.STRING }) invoiceId!: string;
    @Column({ dataType: DataTypes.STRING }) glAccountId!: string;
    @Column({ dataType: DataTypes.STRING }) material!: string;
    @Column({ dataType: DataTypes.INTEGER }) cantidad!: number;
    @Column({ dataType: DataTypes.DECIMAL }) importe!: number;
    @Column({ dataType: DataTypes.DATE }) createdAt!: Date;
    @Column({ dataType: DataTypes.DATE }) updatedAt!: Date;

    @BelongsTo(() => InvoiceOData, { relation: [{ foreignKey: "id", sourceKey: "invoiceId" }] })
    invoice!: InvoiceOData;

    @BelongsTo(() => GlAccountOData, { relation: [{ foreignKey: "id", sourceKey: "glAccountId" }] })
    glAccount!: GlAccountOData;
}
```

## 2. Resto del módulo

- `interface/invoiceitem.interface.ts` → `IInvoiceItem`.
- `dto/invoiceitem.dto.ts` → `InvoiceItemCreateDTO` / `InvoiceItemUpdateDTO` (valida cantidad >= 1, importe >= 0).
- `controller/invoiceitem.odata.controller.ts` → `ODataControler` (`allowedMethod: ["get", "post", "put", "delete"]`).
- `service/invoiceitem.service.ts` → singleton con CRUD completo.
- `main.ts` → barrel.

## 3. Registro

- `datasource.ts`: añadir `InvoiceItemOData`.
- `odata.service.ts`: añadir `new InvoiceItemODataController()`.
- `invoice.odata.model.ts`: añadir `@HasMany(() => InvoiceItemOData, ...)` inverso.
- `glaccount.odata.model.ts`: añadir `@HasMany(() => InvoiceItemOData, ...)` inverso.

## 4. Criterios de aceptación

- [x] `src/core/finance/invoiceitem/` completo.
- [x] `@HasMany` inverso en Invoice y GlAccount.
- [x] `pnpm test` en verde.

➡️ [`f1.8-payment.md`](f1.8-payment.md)
