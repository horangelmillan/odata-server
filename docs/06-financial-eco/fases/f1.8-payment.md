# F1.8 — Dominio `payment` (Pago / transacción con clearing)

> **Sub-fase:** F1.8 · **Esfuerzo:** Medio · **Patrón:** `docs/02-patrones/05-odata-module-pattern.md`
> **Depende de:** F1.5.

## 0. Objetivo

Crear `src/core/finance/payment/` — pago/transacción financiera que referencia a `invoice`.

## 1. Modelo `src/core/finance/payment/model/payment.odata.model.ts`

```typescript
@Table({ tableName: "payments", timestamps: true })
export class PaymentOData extends Model<PaymentOData> {
    @Column({ dataType: DataTypes.STRING, isPrimaryKey: true }) id!: string;
    @Column({ dataType: DataTypes.STRING }) invoiceId!: string;
    @Column({ dataType: DataTypes.DATE }) fecha!: string;
    @Column({ dataType: DataTypes.DECIMAL }) importe!: number;
    @Column({ dataType: DataTypes.STRING }) metodo!: string;
    @Column({ dataType: DataTypes.DATE }) createdAt!: Date;
    @Column({ dataType: DataTypes.DATE }) updatedAt!: Date;

    @BelongsTo(() => InvoiceOData, { relation: [{ foreignKey: "id", sourceKey: "invoiceId" }] })
    invoice!: InvoiceOData;
}
```

## 2. Resto del módulo

- `interface/payment.interface.ts` → `IPayment`.
- `dto/payment.dto.ts` → `PaymentCreateDTO` / `PaymentUpdateDTO` (valida importe >= 0).
- `controller/payment.odata.controller.ts` → `ODataControler` (`allowedMethod: ["get", "post", "put", "delete"]`).
- `service/payment.service.ts` → singleton con CRUD completo.
- `main.ts` → barrel.

## 3. Registro

- `datasource.ts`: añadir `PaymentOData`.
- `odata.service.ts`: añadir `new PaymentODataController()`.

## 4. Criterios de aceptación

- [x] `src/core/finance/payment/` completo.
- [x] `pnpm test` en verde.

➡️ [`f2-seed-remontable.md`](../f2-seed-remontable.md)
