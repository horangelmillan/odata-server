# F1.6 — Dominio `supplierinvoice` (Factura proveedor / MM)

> **Sub-fase:** F1.6 · **Esfuerzo:** Medio · **Patrón:** `docs/02-patrones/05-odata-module-pattern.md`
> **Depende de:** F1.3.

## 0. Objetivo

Crear `src/core/finance/supplierinvoice/` — factura de proveedor (MM), análoga a `invoice` pero con `supplier`.

## 1. Modelo `src/core/finance/supplierinvoice/model/supplierinvoice.odata.model.ts`

```typescript
@Table({ tableName: "supplierinvoices", timestamps: true })
export class SupplierInvoiceOData extends Model<SupplierInvoiceOData> {
    @Column({ dataType: DataTypes.STRING, isPrimaryKey: true }) id!: string;
    @Column({ dataType: DataTypes.STRING }) supplierId!: string;
    @Column({ dataType: DataTypes.DATE }) fecha!: string;
    @Column({ dataType: DataTypes.DECIMAL }) importe!: number;
    @Column({ dataType: DataTypes.STRING }) moneda!: string;
    @Column({ dataType: DataTypes.STRING }) estado!: string;
    @Column({ dataType: DataTypes.DATE }) createdAt!: Date;
    @Column({ dataType: DataTypes.DATE }) updatedAt!: Date;

    @BelongsTo(() => SupplierOData, { relation: [{ foreignKey: "id", sourceKey: "supplierId" }] })
    supplier!: SupplierOData;
}
```

## 2. Resto del módulo

- `interface/supplierinvoice.interface.ts` → `ISupplierInvoice`.
- `dto/supplierinvoice.dto.ts` → `SupplierInvoiceCreateDTO` / `SupplierInvoiceUpdateDTO`.
- `controller/supplierinvoice.odata.controller.ts` → `ODataControler` (`allowedMethod: ["get", "post", "put", "delete"]`).
- `service/supplierinvoice.service.ts` → singleton con CRUD completo.
- `main.ts` → barrel.

## 3. Registro

- `datasource.ts`: añadir `SupplierInvoiceOData`.
- `odata.service.ts`: añadir `new SupplierInvoiceODataController()`.
- `supplier.odata.model.ts`: añadir `@HasMany(() => SupplierInvoiceOData, ...)` inverso.

## 4. Criterios de aceptación

- [x] `src/core/finance/supplierinvoice/` completo.
- [x] `@HasMany` inverso en Supplier.
- [x] `pnpm test` en verde.

➡️ [`f1.7-invoiceitem.md`](f1.7-invoiceitem.md)
