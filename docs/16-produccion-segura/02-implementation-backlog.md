# 02 — Implementation Backlog: Ciclo 16 — Producción Segura

> Única fuente de verdad de hallazgos del ciclo 16. Categorías según AGENTS.md:
> Riesgo (`R`), Mejora (`M`), Refactorización (`RF`), Deuda Técnica (`DT`),
> Investigación Futura (`IF`), Decisión Arquitectónica (`DA`).
> Estados válidos: Pendiente · En evaluación · Implementado · Descartado ·
> Movido a iniciativa futura · Superseded.

---

## Hallazgos iniciales (investigación 2026-07-31)

| ID | Categoría | Hallazgo | Detalle / evidencia | Impacto | Estado | Resolución |
|---|---|---|---|---|---|---|
| R1 | Riesgo | `pnpm start` / Docker prod roto | `node ./dist/server.js` → `ERR_UNSUPPORTED_DIR_IMPORT`: `dist/index.mjs` de `@phrasecode/odata` importa 4 directorios sin índice (`./controller`, `./core`, `./decorators`, `./routers`). Dev funciona solo por el loader ts-node/esm | Alto — bloquea producción hoy | Implementado | F3 (puente `odata-runtime.ts` → build CJS vía `createRequire`; sin tocar la librería) |
| R2 | Riesgo | Migraciones nunca se aplican en prod | Migrator Umzug con `glob: ["[0-9]*.ts", …]`; en `dist/` son `.js` → pending vacío; `sync()` lo cubre hoy silenciosamente | Medio — bug latente de esquema | Implementado | F3 (lista explícita de F1 + verificado en BD limpia desde dist y dentro de Docker) |
| R3 | Riesgo | CORS sin restricción de origen | `main.ts` usa `cors({ exposedHeaders })` sin `origin` → abierto a cualquier origen incluso en prod | Medio | Implementado | F2 (callback `origin === CORS_ORIGIN`) |
| R4 | Riesgo | `SECRET_KEY` con default "change-me" | `env.config.ts:39`; sin validación ni fail-fast en prod; JWT muerto desde D1 (ciclo 15) | Alto en prod | Implementado | F2 (fail-fast ≥32 chars) |
| R5 | Riesgo | CI no ejecuta el dist/start | El CI solo hace build+type-check+tests; el start se rompió sin detección | Medio | Implementado | F3 (paso "Smoke start (dist, modo produccion)") |
| M1 | Mejora | Seguridad por entorno (open/strict) | dev/test abiertos sin configurar claves; prod estricto (auth, CORS, rate-limit, CSP) | Alta | Implementado | F2 |
| M2 | Mejora | `/healthz` + healthcheck prod | Liveness (ping BD + uptime) público; wiring en `docker-compose.prod.yml` | Baja | Implementado | F2/F3 (endpoint F2; healthcheck compose + `CORS_ORIGIN` + `DB_SSL=false` en F3) |
| M3 | Mejora | Docs de seguridad reescritas | `docs/03-seguridad-datos/07-…` describe la era REST; `.env.example` sin `CORS_ORIGIN` ni política de `SECRET_KEY` | Baja | Implementado | F2 |
| RF1 | Refactorización | `common` importa `core` | `odata-models.ts` (11 modelos de dominio) + `odata.service.ts` (`domainRegistrations` de `core/main.js`). Violación de modularidad: eliminar un dominio rompe common | Alta | Implementado | F1 |
| RF2 | Refactorización | Migraciones finance en common | `src/common/service/odata/migrations/002-rich-financial-model.ts` y `003-supplierinvoice-items-payments.ts` son contenido del dominio finance | Media | Implementado | F1 |
| RF3 | Refactorización | Bootstrap como composición única | `src/main.ts` como único punto que conoce core y common; modelos viajan en cada registro de dominio | Media | Implementado | F1 |
| DT1 | Deuda Técnica | Deps huérfanas bcrypt/jsonwebtoken | Permanecen en `dependencies` desde la era REST sin consumidores (se re-utilizan en F2 con el dominio auth) | Baja | Implementado | F2 (re-utilizadas por el dominio auth) |
| DT2 | Deuda Técnica | Índice §15 obsoleto | `docs/00-indice.md` §15 dice "Ciclo en ejecución… pendiente de aprobación" — ya está mergeado (PR #31) | Baja | Pendiente | F5 |
| DT3 | Deuda Técnica | Checklist de patrones obsoleta | `docs/02-patrones/10-best-practices-checklist.md` línea 56: "Sin JWT/bcrypt activos… se re-creará con requisito real" — el requisito real llega en este ciclo | Baja | Pendiente | F5 |
| DT4 | Deuda Técnica | Referencia de arquitectura desactualizada | `docs/01-fundamentos/01-odata-architecture-reference.md`: Node 20, REST en el pipeline, estructura pre-refactor | Baja | Pendiente | F5 |
| DT5 | Deuda Técnica | Write services usan registro global en bootstrap | Enlazado vía `registerDataSource()`/`getDataSource()` en vez de DI estricta (trade-off de mínima modificación; error claro si no enlazado). Se revisa si la arquitectura lo exige | Baja | Implementado | F1 |
| IF1 | Investigación Futura | Observabilidad externa | Métricas (prom-client), APM, logging estructurado con transporte — sin operación real que lo requiera (YAGNI) | Baja | Movido a iniciativa futura | — |
| IF2 | Investigación Futura | Rate-limit: librería vs in-memory | Evaluar con Context7 `express-rate-limit` vs limiter propio en F2; si la dependencia es mínima se incorpora | Baja | Implementado | F2 (`express-rate-limit`, 0 deps de runtime) |
| DA1 | Decisión Arquitectónica | Endpoints públicos en prod | Propuesta: `/healthz` y `$metadata` públicos; resto de `/odata` requiere Bearer. Se confirma en F2 | Media | Implementado | F2 (confirmada por el usuario) |
| RF4 | Refactorización | `GET /healthz` no existe | El server no expone `/healthz` (404 en dev y prod); se requiere para liveness/healthcheck (M2). Se implementa en F2 con el ruteo de seguridad | Baja | Implementado | F2 |
| R6 | Riesgo | Dockerfile prod: sin `pnpm-lock.yaml` en stage production | `COPY package.json` no incluía el lockfile → `--frozen-lockfile` imposible (`ERR_PNPM_NO_LOCKFILE`). Detectado y corregido en F3; registrado aquí en F4 | Medio | Implementado | F3 (copia de lockfile en stage production) |
| R7 | Riesgo | Dockerfile base `node:20.18.0` vs `engines: ">=22 <23"` | Inconsistencia con CI (22.20.0) y warning de pnpm. Detectado y corregido en F3; registrado aquí en F4 | Bajo | Implementado | F3 (base `node:22.20.0-alpine`) |
| M4 | Mejora | Criterio de determinismo del dataset para `db:reset` ×2 | Con el dominio auth (F2), `users.passwordHash` no es determinista entre ejecuciones (salt bcrypt aleatorio por diseño de seguridad). El md5 de F4 excluye auditoría (`createdAt`/`updatedAt`) y `passwordHash`; el resto es determinista (`05d92a3c…` idéntico ×2) | Baja | Implementado | F4 (criterio documentado en `f4-validacion-integral.md` §3.2) |
| R8 | Riesgo | Smoke de CI frágil: comparte `odata_dev` con los tests | Con el PR #32 abierto, el CI real reveló: tras `pnpm test` el esquema queda sincronizado (sync dev) sin `SequelizeMeta`, y el smoke (prod) re-ejecutaba la migración 002 (`ALTER ADD COLUMN docNumber`, no idempotente) → columna existente → server abortaba → healthz nunca 200. En F3 el paso se validó localmente, no en un run del pipeline | Medio | Implementado | F4 (ci.yml: `DROP SCHEMA public CASCADE; CREATE SCHEMA public` antes de arrancar + usuario de humo creado tras el arranque, tabla `users` nace con la migración 004; flujo replicado localmente) |

---

## Bitácora

| Fecha | ID | Acción |
|---|---|---|
| 2026-07-31 | — | Ciclo creado (F0): rama `feat/produccion-segura`, plantilla de ciclo (D8), plan maestro, este backlog, índice §16 |
| 2026-07-31 | RF1, RF2, RF3, DT5 | F1 ejecutada: datasource factory+registro, `createODataExpressApp` como factory, composición en `main.ts`/`server.ts`, migraciones finance movidas al dominio (identidad `SequelizeMeta` preservada con extensión — el resolver glob histórico registraba `.ts`), `odata-models.ts` eliminado, migrator con lista explícita `KernelMigration[]`. Gate: build ✅, tsc test ✅, 188/188 ✅, grep 0 imports core en common ✅, smoke dev ✅ (migraciones reconocidas como aplicadas; `$metadata` 200). Hallazgo nuevo: RF4 (`/healthz` ausente → F2) |
| 2026-07-31 | R3, R4, M1, M2, M3, IF2, DA1, RF4, DT1 | F2 ejecutada: seguridad por entorno (dev/test abiertos; prod fail-fast: `SECRET_KEY` ≥32 y `CORS_ORIGIN` obligatorias), dominio `src/core/auth/` completo (modelo `AuthUserOData`/`users`, migración `004-auth-users.ts`, `authService.login` bcrypt+JWT, `POST /auth/login`), `AuthMiddleware.requireBearerToken` + `HealthzMiddleware` (público, resolver perezoso), CORS por callback (`origin === CORS_ORIGIN`), rate-limit escrituras `express-rate-limit` (IF2; 100/15min/IP), scripts `pnpm auth:create-user`/`seed:auth`/`db:reset` encadenado, `DB_SSL` toggle, docs/03 reescritas. Gate: build ✅, tsc test ✅, **215/215** ✅ (27 tests nuevos: env-validation, auth service/middleware/controller, security.api dev, security-prod.api con `vi.hoisted`), smoke dev ✅ (migración 004 aplicada, healthz 200, `/odata` 200 sin token, login OK), smoke prod ✅ (fail-fast ×3, healthz+`$metadata` públicos, 401 sin token, login→token→200). Pendiente conocido: `pnpm start` (dist) sigue roto por R1 → F3 |
| 2026-07-31 | R1, R2, R5, M2 | F3 ejecutada: arranque productivo. R1 → `src/common/service/odata/odata-runtime.ts` (puente `createRequire` → build CJS de `@phrasecode/odata`, la misma que usa dev y donde viven los parches; la build ESM del paquete tiene imports sin extensión/dir → `ERR_UNSUPPORTED_DIR_IMPORT`); imports de valores migrados (13 modelos, 12 controllers, datasource, odata.service, batch.middleware); tipos siguen como `import type` del paquete. R2 → verificado: desde dist aplican 001-004 en BD limpia (`odata_f3_smoke`) y dentro del contenedor. R5 → paso "Smoke start (dist, modo produccion)" en ci.yml (healthz 200, 401 sin token, login→200). M2 → compose prod: healthcheck `/healthz`, `CORS_ORIGIN` `:?` obligatoria, `DB_SSL=false`. Dockerfile: corregida copia de `pnpm-lock.yaml` en stage production (ERR_PNPM_NO_LOCKFILE) y base `node:22.20.0-alpine` (engines ≥22). Gate: build ✅, tsc ✅, 215/215 ✅, smoke dist dev/prod ✅, smoke BD limpia ✅, **Docker prod up healthy ✅** (healthz/metadata 200, 401, login→200) |
| 2026-08-01 | R6, R7, M4 | F4 ejecutada: validación integral. Rama publicada + **PR #32** abierto (antes solo existía en local; GIT_WORKFLOW §7, plan §4). Gate estático ✅ (build, tsc tests, 215/215). Determinismo ✅ (`db:reset` ×2 → md5 `05d92a3c…` idéntico; criterio excluye auditoría + `passwordHash` — salt bcrypt por diseño; M4). Smoke dist ✅ (dev: healthz+metadata; prod: públicos 200, 401 sin token, login→200). Playwright ✅ (bench 8/8 PASS incl. `$batch` changeset; navegación Demo ↔ Finance ↔ detalle I00142 con líneas+Cuenta Mayor; Atrás OK; 0 errores consola). R6/R7 (bugs Dockerfile de F3) registrados aquí como Implementado. Header del plan maestro e índice §16 actualizados al estado real |
| 2026-08-01 | R8 | **CI del PR #32 rojo** en "Smoke start": el smoke compartía `odata_dev` con los tests (esquema sincronizado sin `SequelizeMeta`) → la migración 002 chocaba (`docNumber` ya existente) → server abortaba. Fix en `ci.yml` (DROP SCHEMA antes de arrancar + `auth:create-user` tras el arranque) replicado localmente en su totalidad: healthz 200 → ci-smoke → 401 → login → token → 200. El fallo no se detectó en F3 porque el paso se validó sin un run del pipeline — el PR abierto en F4 lo expuso |
